function reverseArray(arr) {
    return arr.reverse(); // reverses the original array
}

// Example usage
let numbers = [10, 20, 30, 40, 50];
let reversed = reverseArray(numbers);

console.log(reversed); // [50, 40, 30, 20, 10]
